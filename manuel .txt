
Ouvrir Anaconda.navigator et aller dans Envrionnements, en bas à gauche cliquer sur "Import",
dans "Local drive, ouvrir le fichier R451.yml

Dans enviornnement, verifier que l'environnement "r451_env" a bien été créer, et cliquer sur l'environnement sur la petite fleche verte, et ouvrir un terminal.

Dans le terminal de l'environnement "r451_env", executer les commandes ligne par ligne  :

"""
  R
  install.packages("IRkernel")    #choisir un cran  , ex : 1
  IRkernel::installspec(name = 'ir-r451', displayname = 'R 4.5.1')
"""
fermer le terminal, retrouner sur Home, et installer jupyter notebook, puis lancer le.

Ouvrir le jupyter notebook : "alignement_sur_r.ipynb"

lancer le jupyter notebook case par case, 
